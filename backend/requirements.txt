# ============================================================
# BookMySeat — Python dependencies (Vercel serverless ready)
# Pinned exactly so every deploy is reproducible.
# ============================================================

# --- Web framework ---------------------------------------------------------
Django==6.0.7
asgiref==3.12.1
sqlparse==0.5.5

# --- Environment / configuration -------------------------------------------
# Imported by bookmyseat/settings.py (load_dotenv + DATABASE_URL).
python-dotenv==1.0.0
dj-database-url==3.1.2

# --- Static files ----------------------------------------------------------
# Serves collected static files in production (no separate web server).
# Added to MIDDLEWARE in bookmyseat/settings.py; works on serverless.
whitenoise==6.8.2

# --- Database --------------------------------------------------------------
# PostgreSQL driver used by dj-database-url when DATABASE_URL points at
# PostgreSQL (Render / Railway / Vercel). Compatible with Python 3.12/3.13
# and Django 6.0. Local development keeps using SQLite (no driver needed).
psycopg2-binary==2.9.12

# --- Cache -----------------------------------------------------------------
# Optional Redis cache (used only when REDIS_URL is set; falls back to
# LocMem otherwise). Pulls in `redis` automatically.
django-redis==5.4.0

# --- Imaging / PDF tickets / QR codes --------------------------------------
Pillow==12.0.0
reportlab==4.2.2
qrcode==8.2

# --- Payments --------------------------------------------------------------
razorpay==2.0.1

# --- Utilities -------------------------------------------------------------
typing_extensions==4.15.0

# --- App server -------------------------------------------------------------
# Gunicorn is the WSGI server used in production (Render). Vercel does not
# need it (it wraps the WSGI app itself), but it must stay installed for
# Render-style deployments.
gunicorn==23.0.0
